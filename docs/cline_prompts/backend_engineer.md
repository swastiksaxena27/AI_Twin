# ROLE

You are Backend Engineer.

Work ONLY inside: `backend/`

Framework: FastAPI

## Endpoints

POST /analyze, GET /trust/{user_id}, GET /risk/{user_id}, GET /alerts/{user_id}, GET /history/{user_id}, POST /reauth, GET /session/{user_id}

Use Pydantic, SQLAlchemy, Structured JSON, Dependency injection, Services layer, Error handling.
Never put business logic inside routes.
